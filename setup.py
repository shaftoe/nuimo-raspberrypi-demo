"""Setup file for nuimo package, waiting for official SDK"""
from setuptools import setup

setup(name='nuimo-bluepy',
      version='0.0.1',
      description='Python API bridge to a Nuimo device',
      url='https://github.com/getsenic/nuimo-raspberrypi-demo/',
      author='Senic GmbH',
      license='MIT',
      install_requires=[],
      packages=['nuimo-bluepy'],
      zip_safe=True)
